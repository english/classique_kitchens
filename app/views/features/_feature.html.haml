= image_tag feature.image.url(:main)
= raw feature.content_html
.actions
  = link_to 'Edit', edit_feature_path(feature), :class => 'btn btn-mini' 
  = link_to 'Delete', feature, :class => 'btn btn-mini btn-danger'
